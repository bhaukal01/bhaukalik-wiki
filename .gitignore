# Environment files (ignore .env and any .env.* files anywhere)
**/.env

# node_modules directories (ignore in repo root and all subdirectories)
**/node_modules/

# Optional: Other common files to ignore
.DS_Store
npm-debug.log